<main class="container index">
	
	<div class="row">

		<div class="<%= css[:col_class] %>">
			
			<%= render 'hero_a' %>

			<%= render 'features' %>

			<%= render 'screenshot' %>

			<%= render 'social_proof' %>

			<%= render 'faqs' %>

			<%= render 'pricing_blurb' %>

		</div>

	</div>

</main>
